sudo: true
dist: trusty
cache: false
env:
- URL=https://developer.salesforce.com/media/salesforce-cli/sfdx-linux-amd64.tar.xz
before_install:
- openssl aes-256-cbc -K $encrypted_66ab407c2d24_key -iv $encrypted_66ab407c2d24_iv
  -in assets/new_server.key.enc -out assets/new_server.key -d
- export SFDX_AUTOUPDATE_DISABLE=true
- export SFDX_USE_GENERIC_UNIX_KEYCHAIN=true
- export SFDX_DOMAIN_RETRY=300
- wget -qO- $URL | tar xJf -
- "./sfdx/install"
- export PATH=./sfdx/$(pwd):$PATH
- sfdx plugins:install salesforcedx@pre-release
- sfdx force:auth:jwt:grant --clientid $CONSUMERKEY --jwtkeyfile assets/new_server.key
  --username $USERNAME --setdefaultdevhubusername -a DevHub
script:
- sfdx force:org:create -v DevHub -s -f config/project-scratch-def.json -a ciorg
- sfdx force:source:push -u ciorg
- sfdx force:apex:test:run -u ciorg -c -r human
- sfdx force:org:delete -u ciorg -p
